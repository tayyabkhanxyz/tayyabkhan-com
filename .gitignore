.DS_Store
node_modules/
.wrangler/
.dev.vars

# large source image, kept locally only
profile picture homepage.jpg

# review masters (734MB) — web versions live in assets/video/
Client review/

# square /about master (12MB) — web version is assets/video/profile.mp4
Profile Video.mov
